{
  "name": "@ai-novel-diagnosis/ai-core",
  "version": "0.1.0",
  "description": "Shared analysis contracts for AI网文诊断台 triage, critique, full-book assets, and graph review.",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsdown --watch",
    "build": "tsdown",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "format": "oxfmt --check package.json tsconfig.json tsdown.config.ts vitest.config.ts src",
    "format:fix": "oxfmt --write package.json tsconfig.json tsdown.config.ts vitest.config.ts src",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run format",
    "check:fix": "pnpm run lint:fix && pnpm run format:fix",
    "prepublishOnly": "pnpm run check && pnpm run test && pnpm run build",
    "publish:public": "pnpm publish --access public"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "@vitest/coverage-v8": "^4.0.18",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "tsdown": "^0.20.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@10.14.0"
}
